{
  "name": "@sti/info",
  "type": "module",
  "private": true,
  "version": "0.0.0",
  "description": "info.sti.care, the static STI education library (doc 34)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint:voice": "node scripts/voice-lint.mjs",
    "lint:styles": "node scripts/style-lint.mjs",
    "test": "npm run check && npm run lint:voice && npm run lint:styles",
    "test:build": "node --test test/build.test.mjs",
    "test:visual": "./scripts/visual-regression.sh",
    "test:visual:update": "./scripts/visual-regression.sh --update"
  },
  "dependencies": {
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.6.3"
  }
}
