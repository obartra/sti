node_modules/
*.log
.DS_Store
.netlify/

# Prototype zips dropped in the repo root for deploy/ — never commit these.
/*.zip

# The assembled publish dir (deploy/build-site.sh): app + /promises + /design.
dist/

# Internal-only generated reports (deploy/build-behaviors.mjs): never published.
reports/

# Local SQLite dev database (created when the Go server runs locally).
server/sti.db*

# Playwright run artifacts (also ignored under passport/, but runs can drop
# this at the repo root depending on cwd).
test-results/
